interface:
  display_name: "Repro Check"
  short_description: "Review reproducibility before sharing results and hand back blockers."
  default_prompt: "Use $repro-check to update `research/reviews/repro_checklist.md`, list any blockers, and name the next owner before these results are shared or written up."

policy:
  allow_implicit_invocation: false
