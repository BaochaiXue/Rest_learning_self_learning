interface:
  display_name: "Paper Drafting"
  short_description: "Draft prose from verified artifacts only."
  default_prompt: "Use $paper-drafting to turn verified findings and the claims registry into a cautious draft with citation placeholders, explicit evidence gaps, and a handoff if the draft exposes missing proof."

policy:
  allow_implicit_invocation: false
