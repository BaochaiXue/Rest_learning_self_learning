interface:
  display_name: "Literature Triangulation"
  short_description: "Compare sources, register evidence-backed claims, and hand off gaps."
  default_prompt: "Use $literature-triangulation to update `research/literature_map.md` and `research/claims_registry.md` with primary vs secondary sources, verified vs uncertain claims, and a handoff for the next reviewer."

policy:
  allow_implicit_invocation: true
