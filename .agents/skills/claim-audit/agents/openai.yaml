interface:
  display_name: "Claim Audit"
  short_description: "Check whether claims are actually supported."
  default_prompt: "Use $claim-audit to review these draft claims against `research/claims_registry.md`, findings, and source pointers before they go into a summary or paper section."

policy:
  allow_implicit_invocation: true
