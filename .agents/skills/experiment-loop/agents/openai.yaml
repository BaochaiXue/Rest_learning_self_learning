interface:
  display_name: "Experiment Loop"
  short_description: "Execute, verify, log, and hand off the next step."
  default_prompt: "Use $experiment-loop to run the smallest useful experiment, update `research/findings.md`, refresh `research/experiment_queue.csv`, and leave a concise handoff."

policy:
  allow_implicit_invocation: true
